export enum Environment {
  PRODUCTION = 'production',
  DEVELOP = 'develop',
  LOCAL = 'local',
  TEST = 'test',
}

export enum TeamEnum {
  BUSINESS_ACCELERATION = 'business_team',
  VENDOR_ACCELERATION = 'vendor_team',
  FULFILLMENT = 'fulfillment_team',
}
